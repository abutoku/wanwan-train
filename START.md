# Wan Wan Train 
プロジェクト名: Wan Wan Train 

## 概要
東京の路線図アプリ
ドット絵のチワワ2匹が乗った電車が移動できるプロダクト

## 何のためのアプリケーションか
改札通った後に乗る路線、行きたい駅名はわかるがどちら行き（向き）に乗ればいいのかわからないことがある
路線図を把握することでホームを間違えることをなくす
さらにあと何駅で到着するかのイメージもできる

## 想定ユーザー
- チワワが好きな東京への旅行者・出張者

## 同様のアプリケーションとの差別化
- チワワの可愛さが唯一無二の強みである

## 基本仕様
- imagesフォルダの中の画像を仕様
- 電車に乗っているチワワはuru.png,roi.pngを使用
- タイトル画面はwanwan.pngを使用
- ゲーム画面のようなUI
- レスポンシブ対応
- テストコードの実装は不要
- 路線のカラーは忠実に再現
- 自動で進む／ボタン操作の3つがあり、駅をタップするとそこまで移動する

## 第1フェーズ
まずは山手線のみ、東京駅スタート
停車駅の乗り換え案内情報を表示する
内回り、外回りがわかるように

## 第2フェーズ
東京メトロ、東急、など他の路線を追加していく
各駅て乗り換え路線を選択し乗換できるようにする

## 第3フェーズ
タイトル画面でスタート駅を選択できるようにする
フリーワードで駅名検索
→ 複数ある場合はそこから選ぶ
例）渋谷 → 部分一致で複数ヒット、JR渋谷駅、東横線渋谷駅、東京メトロ銀座線渋谷駅

## 第4フェーズ
快速、特快などの停車駅がわかるようにする

## 第5フェーズ
デプロイ

## 第6フェーズ
- PWA化

## 第7フェーズ
自動デプロイ実装
mainブランチにマージしたら本番に反映する

## 第8フェーズ
- 挙動の変更
- 駅をタップ(クリック)したときは一駅ずつ止まらず一気に目的地まで進む
- 各箇所「ドット絵の」という文言を削除、
- タイトルのテキストを修正「チワワの」を削除、「旅へ。」も不要「めぐろう。」で締める
- タイトルのどちら行きに~の行は不要
- タイトルの平仮名を修正、しゅっぱつ駅 → 出発駅 駅名でけんさく → 駅名で検索

## 第9フェーズ
路線の追加
- 都営地下鉄大江戸線
- 都営地下鉄浅草線
- 東京メトロ
  - 半蔵門線
  - 副都心線
- みなとみらい線
- りんかい線
- 田園都市線
- 湘南新宿ライン
- 京急線
- 横須賀線
- 京浜東北・根岸線


## 第10フェーズ
チワワに特化した可愛い演出を追加する
※ここの仕様は現時点で未定


## 実装の進め方
フェーズごとに区切って進める
着手前に不明点があれば確認して開始する
着手前に./docs配下に実装ドキュメントを作成しそれにそって実装を行う
完了後に実装ドキュメントと差分がでたら更新する
各フェーズが完了したらREADME.mdを更新する

---

## 推奨技術スタック

### 結論（推奨構成）
| 領域 | 推奨 | ひとこと |
|---|---|---|
| 言語 | **TypeScript** | 駅・路線・乗換データを型で守る |
| フレームワーク | **React + Vite**（SPA） | サーバー不要・HMRが速くアニメ調整が快適 |
| スタイリング | **Tailwind CSS** | レスポンシブを素早く。`image-rendering: pixelated` でドット絵の質感を維持 |
| アニメーション | **Framer Motion**（または素のCSS transform） | 電車の移動・停車を宣言的に表現 |
| 状態管理 | **Zustand** | 路線／内回り外回り／現在駅／乗換選択を軽量に管理 |
| ルーティング | **React Router** | タイトル→ゲーム→駅検索の画面遷移（第3フェーズ用） |
| データ | **静的JSON（型付き）** | 路線・駅マスタ。DBは当面不要 |
| デプロイ | **Cloudflare Pages**（第一候補） | 静的配信。無料枠が広くCDNが速い |

### なぜこの構成か（理由）
- **React + Vite（Next.jsではなく）**：このアプリはSSRやSEO、サーバー処理が要らない「1画面のインタラクティブなゲーム」。Next.jsはオーバースペックになりがち。Viteはビルド・HMRが高速で、電車アニメの微調整サイクルを速く回せる。将来バックエンドが必要になっても後からNext.js等へ移行可能。
- **TypeScript**：駅・路線・乗換・内回り/外回りといった構造化データを扱うため、型で「駅名の打ち間違い」「乗換先の指定漏れ」などをコンパイル時に防げる。データが増える第2フェーズ以降で効く。
- **Tailwind CSS**：「レスポンシブ対応」「ゲーム画面のようなUI」を最小記述で実現。ドット絵は `image-rendering: pixelated` でボケずに拡大表示できる。
- **Framer Motion**：電車が駅間をスムーズに移動→停車、といった動きを少ないコードで書ける。パフォーマンスが厳しくなればCanvas描画へ切り替える選択肢も残る。
- **Zustand**：Redux ほど重くなく、「今どの路線・どちら向き・何駅目か」というゲーム状態を素直に一元管理できる。
- **静的JSON**：第1〜3フェーズはデータの読み取りのみでDBが不要。路線カラーや停車駅・乗換情報をJSONで持てば、路線追加は「データ追加」で完結する。

### デプロイ先の比較と推奨
| 候補 | 向いている点 | 注意点 |
|---|---|---|
| **Cloudflare Pages（第一候補）** | 無料枠で帯域無制限・世界規模のエッジCDN。画像が重く移動中の旅行者が使う本アプリと好相性。将来はWorkersでAPI追加も可 | 独自の設定に多少慣れが必要 |
| **Vercel（対抗馬）** | DXが最良。将来Next.js化やサーバー処理を足すなら最有力。プレビューデプロイが便利 | 無料枠は帯域に上限あり（画像多用だと注意） |
| **GitHub Pages（最小構成）** | 完全無料・設定が最も簡単。まず動くものを置くなら十分 | 独自ドメインやAPI・リダイレクト等は弱い |

**推奨**：まずは **Cloudflare Pages**。理由は (1) 画像アセットが大きくCDN配信の速さが体感差になる、(2) 想定ユーザーが移動中でモバイル回線・世界各地からのアクセスに強い、(3) 無料枠の帯域が実質青天井、(4) 第2フェーズ以降で駅検索APIなどが必要になってもWorkersで拡張できるため。
Next.js採用に舵を切る場合や、チームのDX重視なら **Vercel** を選べば良い。

### あわせて推奨：PWA化
想定ユーザーが「移動中の旅行者」であることを踏まえ、**PWA（インストール可能＋路線データのオフラインキャッシュ）** を早い段階で検討する価値が高い。電波の弱い駅ホームや地下でも路線図が見られることは、このアプリの実用価値を大きく高める。ViteならプラグインでPWA化が容易。
